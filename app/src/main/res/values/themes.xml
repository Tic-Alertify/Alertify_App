<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Alertify_App" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.Alertify_App" parent="Base.Theme.Alertify_App" />
    
    <!-- Estilos para Bottom Navigation -->
    <style name="CustomActiveIndicator">
        <item name="android:color">@color/alertify_primary_light</item>
        <item name="android:alpha">0.3</item>
    </style>
    
    <!-- Estilo para Tarjetas con bordes rectos abajo -->
    <style name="ShapeAppearance.App.Card.TopRounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">32dp</item>
        <item name="cornerSizeTopLeft">32dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
    </style>
</resources>